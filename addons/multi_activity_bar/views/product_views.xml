<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_product_template_bar_form" model="ir.ui.view">
        <field name="name">product.template.bar.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='list_price']" position="after">
                <field name="ma_is_drink"/>
                <field name="ma_dose_uom_id" invisible="not ma_is_drink"/>
                <field name="ma_bottle_uom_id" invisible="not ma_is_drink"/>
            </xpath>
        </field>
    </record>
</odoo>
