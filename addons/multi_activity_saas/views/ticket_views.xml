<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_ticket_list" model="ir.ui.view">
        <field name="name">multi.activity.ticket.list</field>
        <field name="model">multi.activity.ticket</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="tenant_id"/>
                <field name="category"/>
                <field name="priority"/>
                <field name="user_id"/>
                <field name="state"/>
            </list>
        </field>
    </record>
    <record id="view_ticket_form" model="ir.ui.view">
        <field name="name">multi.activity.ticket.form</field>
        <field name="model">multi.activity.ticket</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="new,in_progress,resolved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="tenant_id"/>
                            <field name="category"/>
                            <field name="priority"/>
                            <field name="user_id"/>
                        </group>
                        <group>
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </group>
                    </group>
                    <field name="description" placeholder="Describe the issue"/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <record id="action_ticket" model="ir.actions.act_window">
        <field name="name">Support Tickets</field>
        <field name="res_model">multi.activity.ticket</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
